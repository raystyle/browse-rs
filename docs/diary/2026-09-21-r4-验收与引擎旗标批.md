# 2026-09-21 r4 引擎验收窗与引擎旗标批（v0.10.0）

## r4 验收窗（clean-chrome 152.0.7977.84-r4，五端全绿）

- waitForResponse 事件面缺陷全链：r3 验收窗发现（四变体零 responseReady，裸 CDP 分界坐实引擎侧），用户令本工位全权接手修复；根因三层（Dispose 清全局桥、跨文档导航整组 agent 连 sink 重建 waits 随葬、daemon 双会话并存下全局桥只指最后 Init 者）；修法三件入 clean-chrome c0bbcfd（桥存活加 wait 注册表进程级加接缝冲排），回归三拓扑绿
- r4 切封回执后五端验收：三端资产 sha256 对账过；wsl 全电池加 e2e 3/3、win 宿主与 lan 三端各自电池（导航流 responseReady 双事件加四函数齐发）；边界实证一条（真跨站换渲染器 wait 不迁移，win 首轮 about:blank 登记跨站一例，双方 diary 在册）
- 镜像 r4 段未上架（latest 仍 r2），五端走 dist 资产本地导入道；R2 上传与 latest 拨 r4 归总台

## 引擎旗标批（#48，REQ-014，61d0297）

- 双通道：--engine-arg 可叠加加 BROWSE_ENGINE_ARGS 空格分隔，spawn_extra_args argv 元素原样直通；求值前置透传纳入触发条件
- 评审两轮（browse-codex-review 快核）：一轮 F1（/engine/up 并集序反致 env 吞显式，评审员实弹钉死）加 G1 死 clone/G2 default 槽/G3 撞车警示，四项全修；F1 修法 env 先行显式随后加同值去重，按原构型复验 argv 序显式胜、单份投递；二轮 CONFIRM 放行
- 实弹：零 wrapper 起 pinned r4，semanticSnapshot 1 节点、登记后导航 responseReady 双事件、负对照 -32601、env 形态同效
- 踩坑一记：surface 投影 llms Commands 节派生自 up CmdSpec 而非 COMPANION_FLAGS，旗标词条漏投投影（契约测试绿是同源假象），up CmdSpec 三点补齐后投影随迁

## 封版 v0.10.0

- 判据：能力新增取 minor（REQ-004）；#48 关单走 omc 委托道（已发单，关单后封）
- 版本头随迁 surface 与 aidoc 投影；三平台产线包（linux 本职加 win-gnu 交叉加 mac 实机）随 tag 发布

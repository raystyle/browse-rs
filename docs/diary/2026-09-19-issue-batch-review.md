# 2026-09-19 issue 整批评审与跨仓对齐

当天事实与裁定：

- issue 台账巡检：15 条（12 open #18 至 #29，加 3 closed），open 全是 09-18 16:10 至 16:30 同批沉淀（首日驾驶循环实测、竞品调研 agent-browser 与 playwright-mcp、源码巡检三流），仓内文档零吸收。
- 用户令整批走 herdr 评审：带起常驻评审格 browse-codex-review（工位 tab 右侧，本仓 cwd），三轮闭环：请求（五件结构）-> 一轮回执 6 CONFIRM / 6 F -> 工位逐锚验证零翻案 -> 修订回执 -> 二轮快核 CONFIRM 终审放行。回执全文落 /tmp/browse-rs-issues-18-29-review.md（评审方临时件，不入册）。
- 一轮 F 载荷主张逐锚验证全中：#18 `\d`/`\s` 静默吞反斜杠（parser.rs 转义 match 的 other 分支）；#19 归因改 Page.enable 时序（wait_for 环形缓冲可取历史、spawn_dialog_watcher 300ms 轮询补开）；#21 auto-parse 证伪，真落点是 member-call bail 加 preview/render_result 字符串裸打（.length 本就是显式特例）；#22 与 ADR-0002 Alternatives 冲突，升 ADR 级；#27 引文 REQ-009/S006/反差分 ADR-0005 归属 clean-chrome（本仓 requirements 实锚 REQ-001 至 REQ-005）；#28 CDP 原生路径在位（methods.txt 的 Browser.grantPermissions:59、setPermission:64、Emulation.setUserAgentOverride:270）；#29 落点改本仓 e2e（pipe-smoke.py 是 Windows-only，WSL 跑不了）。[实证: 评审方回执锚点与本仓源码双锚抽核，零翻案]
- 二轮换锚一处：求值 stdin 的锚是 main.rs 376 至 381 派发与 run_stdin 573（348 是 issue body 的 stdin）。外科建议三条采纳：#25.3 的 --isolated 挪 spawn 线；薄封装批判据改「本仓可闭环、不触跨仓或引擎面」（#24 的 if-changed 与 annotate 是本地像素计算，无 CDP 原语，按旧判据会被误判出批）；#25.2 与 #19 共地基 per-target 钩子（Target.setAutoAttach/setDiscoverTargets 在 methods.txt:622/623 在位未用，可替 300ms 轮询）。
- 终版批次（评审对齐，用户裁定入册后开工）：1 验收补格 #29（patch）；2 渲染与报错带类型 #21(ii)（patch）；3 值模型补面 #21(i)（minor）；4 方言真缺陷 #18 -> #20 -> #19（minor，#20 依赖 3，#19 修时留 per-target 钩子）；5 薄封装批 #23 加 #24 加 #25.2 加 #25.3 的 storageState 往返（minor）；6 生命周期线 #25.1 加 #25.5（proxy 与 self_update 的 HTTPS_PROXY 同口径）加 --isolated；7 凭据线 #25.4 加输出回显脱敏；8 #22 单独 ADR（涉用户裁定，不塞批）；9 跨仓 #28 先 CDP 覆写差分实测（S002）再谈引擎开关、#27 只落近端项（AX depth/分页、CLI 侧 if-changed 与 annotate 对比）；#26 作勘误随 #25 留档，不单独立项。
- 跨仓：clean-chrome#1（GitHub）是能力分界对面载体（判据见该 issue 正文，不在本仓复述；#28 对面已改判 daemon 层，与本仓终审同向）。飞轮纯讨论轮对齐单已插队派 clean-chrome 工位，三件：分界判据确认、pipe 补验交接与 S008 三态矩阵归属、Browse.* 域衔接面约定；回执已收（下条）。
- 跨仓对齐回执（clean-chrome 工位，纯讨论轮双方未动仓；对面自家 issue #1 亦走了其评审格，回执镜像 /tmp/clean-chrome-issue1-review.md）：三件全 CONFIRM。件一判据同向，对面收紧为「充分三支加必要性证明」，并指出 S002 的 CDP 覆写差分支撑格现状为空，按需立探针补实测不拿推断当必要性。件二 S008 矩阵 pipe/both 格归对面，证据两级互补：pipe 乘 browse spawn 形参格用我方 e2e 断言输出，both 乘直连/flat 格由对面 Windows 原生跑 pipe-smoke.py 实录（其默认 --exe 指旧路径须显式传参，对面评审 G7）；我方 README 注记不候 both 格。件三优先序：item 4 立即在 152-r2 做（纯验收零引擎改动），items 1/2/3/5 挂 155 窗（对面 REQ-012）独立 REQ，前置与我 #27 同句（token/字节与注入痕迹对比数据）；衔接面约定：引擎版到位后 CLI 退化 Browse.* 直通方言 session 域，同方法名同参数面不做第二套，方法面唯一真相是对面 REQ 冻结版、我方落地显式跨仓引用；gating 走 base::FeatureList 默认关（上游 kDevToolsWebMCPSupport 先例），未开得 -32601 时 daemon 回退 stock 路径即我方近端已落那套，不废弃。对面 F3 实锚我仓三处（record.rs:59 startScreencast 帧流、js_host.rs:438/717/738 __browse_ref_gen），我方复核全中，据此立台账 #30（近端瘦身消注入痕，title/url 改 Target.getTargetInfo）。[实证: 我方 grep 三锚复核；对面引文归属勘误（REQ-009/S006/ADR-0005 系其仓编号）双方对齐]
- 批 1 开工（#29）：e2e `exercise()` 补 `navigator.webdriver` 恒 false 断言（通道参数化，port 与 pipe 各验一格），README 版本说明注记回填（删「pipe 态未验」，改 e2e 断言在册）。批 diff 走评审格两轮快核：F 二处修（diary 权威句改指针式；CHANGELOG 追正口径收窄为 port 与 pipe 两通道，both 态属 clean-chrome 侧矩阵，本仓 spawn 面本就二选一）加 G 五条采纳；增强格留档：Emulation.setAutomationOverride(true) 下仍断 false（S008 显式开关场景），后续测试增强批再接。[实证: BROWSE_E2E=1 BROWSE_NO_ATTACH=1 cargo test -p browse-core --test e2e 双通道 35.58s 全绿；fmt、clippy、test、doc 四门禁绿；两轮快核 CONFIRM 放行]

- 批 2 开工并交付（#21(ii)，patch）：`preview()` 类型化短预览（布尔/数字/字符串带引号/容器 JSON 截断，报错与 `print()` 受益）加 `render_result()` 字符串带引号（serde 序列化形态，数字布尔仍裸打、空容器仍不打）加大值落盘双轨（文件写原文，stdout 才带展示引号）；方言错误契约补字符串 receiver 用例。快核四边界 2 CONFIRM 加 2 F：`__dropped` 回执 bytes 从展示长度改为工件字节（raw.len()，消两口径），getting-started 输出样例补引号；评审方预授权单行修免再轮。G 采纳：bytes 语义入 pub 文档加 doctest 回归锁（DROP_THRESHOLD+1 字符串落盘，回执 bytes 严格等于工件长度）。行为变化自觉申报过闸：CLI 字符串结果输出从裸改变带引号。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 全绿；评审方实跑二进制四形态（字符串/JSON 文本串/容器/浮点）验证]

- 批 3 开工并交付（#21(i)，minor）：JSON 命名空间（parse 带坏 JSON CTA、stringify(v, indent?) 两格缩进多行）加值方法面（字符串八法、数组四法，slice 按 UTF-16 单元与 .length 同口径）加类型感知 CTA（STRING_METHODS/ARRAY_METHODS consts 派生，method_face_catalog_lists_all_methods 把 consts 与 surface 目录描述绑死，增删只改一边即红）。快核两轮：一轮 F（includes 数值等值按 serde 形态，[1].includes(1.0) 假阴）修为 json_value_eq 宽等（i64/u64 精确段先比浮点兜底，评审方逐段推演无洞）；二轮新 F（缓办无载体）补本条留痕：哨兵卫生（__host=json/session 哨兵可随变量泄漏与可伪造命中，系 session 既有先例的放大）缓办，归属 #21 后续或 #30 近端批裁。G 全收：四项 JS 偏差进 surface 条目（agent 可查面）、parse_number 补分型 CTA（超范围给 BigInt 出路、裸负号归因形态、1.2.3 有锁）。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 全绿；llms 92/120 帽内；评审方隔离实例实跑探针后下线]

- 批 4 开工并交付（#18，minor）：反引号模板字符串 raw 语义（只有反引号转义与 \${ 两转义，其余反斜杠序列逐字保留，真换行合法）加普通字符串丢字修（\n\t 引号反斜杠照常，其余保留反斜杠，\d 不再静默吞）加双追踪器同步（strip_comments 与 depth_ok 认反引号）。两轮快核：一轮 F（追踪器奇偶坑，评审方实弹复现）修为 scan_raw_close 单一真相（连续反斜杠奇偶判定，解析器与两追踪器三处共用）；途中我方 post-process 再犯同类奇偶错被矩阵用例抓出，改 run 奇偶结算。用户令严格测试四层齐：单元（parser_contract 14 例：闭合奇偶矩阵 0 至 5、转义全表、\$ 矩阵）、集成（模板与 JSON 与值方法面组合流）、验收（e2e 双通道真 V8 过 #18 原文四用例）、A/B（同载荷模板形与旧式手工转义形同值等价锁；首版旧式编码带 shell 直觉被实弹抓红：方言双引号串里反引号本是普通字符）。二轮 CONFIRM 终审放行；评审方复验口径沉淀：先 build 再 CLI 探针，防二进制滞后假红假绿。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 全绿；e2e 双通道 34.85s；评审方重建二进制后逐格实弹对账含奇偶 0 至 5 与复现件]

- 跨仓后续两笔（clean-chrome 工位，2026-09-19 下午）：件二 pipe 补验 Windows 格绿（spawn 直连加生产形参，pipe 态 CDP 往返加 flat 会话 webdriver false 加 9222 不听，both 态双通道加 9222 听；S008 Windows 格其仓已回填，lan 两端 pipe 格候 POSIX 臂 155 窗），我方 e2e 断言输出与其 pipe-smoke 实录（带恒 false 断言加 --no-sandbox 透传，其 commit 03ee43a）双轨互补在案。部署面课题移交：部署位 exe 遭沙箱受限令牌拒访（sandbox_win.cc:804 报 0x5 后 DCHECK FATAL），纯形必崩而生产 spawn 恒带 --no-sandbox 免疫；修复第三案实证 icacls AppContainer ACE（*S-1-15-2-2:(OI)(CI)RX /T）一行收（部署位 r2 纯形加沙箱即活），据此立台账 #32（chrome install 落位补 ACE 加 doctor 检测），三案全谱在其仓 S008 五节。[实证: 对面 schtasks 脱管跑双态全绿；9339 监听实测]

- 批 5 开工并交付（#20 加 #19，minor）：waitForResponse(pattern, ms?)（glob 同 route 族；窗口语义最近命中含历史，前提 Network 域先开，方言无并发可用形态是触发后等待；体等 loadingFinished 再取，体窗 5 秒，失败显式 bodyError）加 responseBody(requestId)（过期 rid 指引进错误链）；#19 换靶同步开域五入口全覆盖（use 臂、new_tab、switch_tab、engine 的 attach_first_page 与 new_tab；防漏口径与高级路径豁免注记写进 ensure_page_enabled 文档），cdp 层留纯策略不上收。两轮快核各出一真 F：body 竞态（命中响应头即取体被静默吞，评审方真慢体 server 复现）修为体完成信号等待；engine 主路径漏挂（评审方全新 daemon 首片段假超时复现）修两行。我方途中自查一个真 bug：事件字段嵌 params.response.*，首版指针漏 params 段致超时，多层探针定位；另踩 pkill -f 自匹配杀自己 shell、e2e 中断残留引擎占 profile 锁两坑。回归锁形态：真网慢体受 WSL 到 Windows 引擎回环边界限制（评审方指正：WSL 侧 Linux r2 产物连 WSL 监听是通的，真网慢体 e2e 日后可在 WSL 用 Linux 产物补格），主锁为 connect_pipes 内存管道假 CDP 对端单测（先拒 No resource 后放体 400ms 时序，另锁永不完体的 bodyError 路径），BODY_WAIT_MS 测试态 800 门控。cdp wait_for 超时补 CTA。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 绿；e2e 双通道 34.99s；评审方真引擎加真慢体 server 实弹复验 F1F2，五入口 grep 对账]

- 日终全量 code review（任务协议：委派即产物，收件人模式；范围 c2eb767 到 5a277f7 六笔整文件集，评审方静态全文读加隔离实例实弹）：5 F 加 6 G 加 6 无发现面加 10 测试盲格，产物 /tmp/browse-rs-day-fullreview-2026-09-19.md（评审方临时件，结论以台账 #33 为载体）。核心洞察是跨批组合死角：#19 逐 session 开域乘 use_target 只 attach 不 detach，多 tab 流程后陈旧 session 三倍重复投递事件（实测 300 请求页 168 条 URL 仅 56 唯一），放大 waitForResponse 跨 tab 误命中与超时 CTA 体积；另捕数字字面量指数下划线静默截断（return 1e5 得 1）、preview 大值全量序列化尖峰。我方五锚复核全中零翻案，立台账 #33（修序 F1 打头 F2F3 同处落地，六笔不回滚前进式）。无发现面六组入册（模板与转义三处口径、普通串转义回归面、渲染与落盘三件、体就绪路径、#19 五入口、cdp 守卫与事件族加投影）。[实证: 评审方私有 daemon 加 Linux r2 产物加本地慢体与多请求 server 实弹，核完清场；use_target 全仓零 detachFromTarget 我方 grep 复核]

- 批 6 开工并交付（#33 修复批，全量评审 5F 加 6G 全收）：F1 use_target 换靶即 detach 旧附（事件三倍重复根除，e2e 锁换靶全走过后一次导航恰一份）；F2 waitForResponse 按活动 session 过滤（管道单测锁他域短窗超时）；F3 超时 CTA 封顶（活动窗计数加去重加最近三条）；F4 指数下划线数字字面量即报（不再静默截断）；F5 preview 有界序列化（头部 8 项加共 N 项尾注，途中自查双省略号撞车与对象括号错配）；G 六条全落（GLOBALS_CTA const 加双向契约测试、responseBody 与 waitForResponse 同形、先剥注释配平、不 trim 保行号、深等披露、CTA 指引）。快核出新 F 一条（本批自引回归：detach 撕断录制帧流且停场落错 session），修为 pin 机制加定向停：cdp Session 纯机制 pin_session/unpin_session，recordStart 钉住录制 session 跨换靶存活，recordStop 定向对本体停加 sessionChanged 回执告警，e2e 实弹锁帧流存活。二轮 CONFIRM 终审放行。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 绿；e2e 双通道 35.57s；评审方独立实弹复验 pin 与对象预览零翻案]
- 批 6 遗留三项（评审二轮新 G，低危）：pin 无超时回收（录制忘收场则钉住 session 持续投递，候兜底解钉或启动新录制时清理）；waitFor/peekEvents/findEvents 无活动 session 过滤（多 tab 或钉住期间可能消费非活动事件，候同口径过滤或 surface 注明）；对象尾注断言已随批补，前两项进下批裁量。

- 批 7 开工并交付（薄封装批 #23 加 #24 加 #25.2 加 #25.3，minor）：交互动词（pressKey 组合键 vkCode 补齐、hoverRef/hoverAt、dblclickRef、dragRef 鼠标序列、keydown/keyup、typeRef 真实按键序列）；截图选项（format jpeg/quality、ifChanged 字节级跳过）与 emulate 视口 UA 仿真；setInitScript 每新文档注入；storageState 往返（Storage.getCookies 加 DOMStorage）。两轮快核：F2 处（initScript 替换叠加与清除无效，修为 sessionId 到 identifier 集合记账加 drain 全撤，即时路径与 watcher 双注册是实雷，实弹三态 [null,1]/[null,null] 验收；screenshot 条目漏更）加 G 五条全收；G5 落地时实弹再捕真 bug（getDOMStorageItems 回包 {entries:[...]} 原解析按顶层数组恒空，同块对照暴露）。跨端口径（用户令）：新面零 cfg(windows)，vkCode 是 CDP 协议字段名跨平台通用，矩阵有头端验收列为批后置项。e2e 验收块：hover 显形、dblclick、Control+a 全选覆盖、typeRef 入 contenteditable（需 role=textbox 进 AX 树）、initScript 三段锁、storageState 双半边往返、ifChanged 二次 skipped、jpeg 后缀、emulate innerWidth。途中实弹坑：data URL 的 # 被 fragment 截断（class 替）、AX 树 StaticText 节点 focus 无效、/tmp 残留假 skipped、组合键带 text 被当字面输入。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 绿；e2e 双通道 41.97s；评审方复跑复现件三态全过零翻案]
- 过程教训（批 7 三处中招，评审方同判）：脚本化字符串替换对 fmt 后代码形态屡次哑火且不报错，靠行为复现才察觉；后续文案与代码改动固定跑「契约测试加关键短语 grep」双检查，已在本批后段执行。遗留（评审 G-lite）：initScript 替换只对活动 session 重注（非活动已附着 session 旧撤新不补，与条目口径不冲突）；init_script_ids 留已 detach sid 条目（下次 remove 失败忽略，无害）。

- 批 8 开工并交付（生命周期线 #25.1 加 #25.5 加 --isolated，minor）：daemon 闲置回收机（BROWSE_IDLE_TIMEOUT 缺省 1 小时 0 关，退引擎不退 daemon，CLI --idle-timeout 随新 daemon 环境注入）；代理直通（EngineSpec 三新字段到 spawn_extra_args 派生 --proxy-server/--proxy-bypass-list，port/pipes 四臂 extra_args，BROWSE_PROXY 同值）；--isolated 隔离态 profile（spawn 落 state/isolated-时间戳，退场即删，清理失败留痕）。三轮快核：一轮 2F（回收机不取单飞槽会撕断在跑求值且 Not connected 兜底重放整段；env 与旗标两态使 BROWSE_PROXY/PROFILE 一会生效一会不生效且求值路径静默吞三旗标）修为单飞槽加双确认、up 缺字段回落 from_env、run_eval 守卫透传；二轮 1F 为我方修复自引（unreachable 在 BROWSE_CDP_WS 在册配置下 panic 撕断请求）修为 env Attach 直通加四元组解构；三轮 CONFIRM 终审放行。实弹验收：idle 三态（Spawned 到 6 秒窗 NotConnected 到 eval 回拉）、长求值不撕不重放、代理旗标入 cmdline、isolated down 后零残留、BROWSE_CDP_WS 下 up 语义错误不 panic。G 全收：isolated 清理留痕、spawn_extra_args 纪律注释（单 flag 直通不转义不拆分）、--isolated 优先级注明、idleMs 用 still_idle、engine_up 双 browse 前缀去重。单测 spawn_extra_args 形锁、e2e 新测 isolated_profile_removed_on_shutdown。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 绿；e2e 三测 44.12s；评审方四边界逐条实弹复验零翻案]
- 过程教训（批 8）：我方两次踩「二进制滞后源码」口径（探针前忘重建 CLI），评审方口径三次验证必要；脚本化替换哑火在批 8 再现一次（isolated 目录块未落），行为复现加 grep 双检查已成本仓纪律。

裁定：issue 定性的权威载体是 issue 台账与后续各批封版 REQ，本篇只记当轮指针与过程；上文批次表里的 patch/minor 是预报，以封版 REQ 定稿为准。后续各批交付走评审闸门，先闸后推；S008 矩阵 pipe/both 格已定归 clean-chrome 工位回填（pipe 格采我方 e2e 断言输出，both 格其 Windows 实录）；对面 issue #1 判据修订与吸收候其用户裁定，届时跨仓引用回我仓终审与 #27/#28/#29/#30 编号。批 2 的 stdout 形态变化封版时 CHANGELOG 记行（评审方 G，封版流程内）。

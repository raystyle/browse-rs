# 2026-09-19 issue 整批评审与跨仓对齐

当天事实与裁定：

- issue 台账巡检：15 条（12 open #18 至 #29，加 3 closed），open 全是 09-18 16:10 至 16:30 同批沉淀（首日驾驶循环实测、竞品调研 agent-browser 与 playwright-mcp、源码巡检三流），仓内文档零吸收。
- 用户令整批走 herdr 评审：带起常驻评审格 browse-codex-review（工位 tab 右侧，本仓 cwd），三轮闭环：请求（五件结构）-> 一轮回执 6 CONFIRM / 6 F -> 工位逐锚验证零翻案 -> 修订回执 -> 二轮快核 CONFIRM 终审放行。回执全文落 /tmp/browse-rs-issues-18-29-review.md（评审方临时件，不入册）。
- 一轮 F 载荷主张逐锚验证全中：#18 `\d`/`\s` 静默吞反斜杠（parser.rs 转义 match 的 other 分支）；#19 归因改 Page.enable 时序（wait_for 环形缓冲可取历史、spawn_dialog_watcher 300ms 轮询补开）；#21 auto-parse 证伪，真落点是 member-call bail 加 preview/render_result 字符串裸打（.length 本就是显式特例）；#22 与 ADR-0002 Alternatives 冲突，升 ADR 级；#27 引文 REQ-009/S006/反差分 ADR-0005 归属 clean-chrome（本仓 requirements 实锚 REQ-001 至 REQ-005）；#28 CDP 原生路径在位（methods.txt 的 Browser.grantPermissions:59、setPermission:64、Emulation.setUserAgentOverride:270）；#29 落点改本仓 e2e（pipe-smoke.py 是 Windows-only，WSL 跑不了）。[实证: 评审方回执锚点与本仓源码双锚抽核，零翻案]
- 二轮换锚一处：求值 stdin 的锚是 main.rs 376 至 381 派发与 run_stdin 573（348 是 issue body 的 stdin）。外科建议三条采纳：#25.3 的 --isolated 挪 spawn 线；薄封装批判据改「本仓可闭环、不触跨仓或引擎面」（#24 的 if-changed 与 annotate 是本地像素计算，无 CDP 原语，按旧判据会被误判出批）；#25.2 与 #19 共地基 per-target 钩子（Target.setAutoAttach/setDiscoverTargets 在 methods.txt:622/623 在位未用，可替 300ms 轮询）。
- 终版批次（评审对齐，用户裁定入册后开工）：1 验收补格 #29（patch）；2 渲染与报错带类型 #21(ii)（patch）；3 值模型补面 #21(i)（minor）；4 方言真缺陷 #18 -> #20 -> #19（minor，#20 依赖 3，#19 修时留 per-target 钩子）；5 薄封装批 #23 加 #24 加 #25.2 加 #25.3 的 storageState 往返（minor）；6 生命周期线 #25.1 加 #25.5（proxy 与 self_update 的 HTTPS_PROXY 同口径）加 --isolated；7 凭据线 #25.4 加输出回显脱敏；8 #22 单独 ADR（涉用户裁定，不塞批）；9 跨仓 #28 先 CDP 覆写差分实测（S002）再谈引擎开关、#27 只落近端项（AX depth/分页、CLI 侧 if-changed 与 annotate 对比）；#26 作勘误随 #25 留档，不单独立项。
- 跨仓：clean-chrome#1（GitHub）是能力分界对面载体（判据见该 issue 正文，不在本仓复述；#28 对面已改判 daemon 层，与本仓终审同向）。飞轮纯讨论轮对齐单已插队派 clean-chrome 工位，三件：分界判据确认、pipe 补验交接与 S008 三态矩阵归属、Browse.* 域衔接面约定；回执已收（下条）。
- 跨仓对齐回执（clean-chrome 工位，纯讨论轮双方未动仓；对面自家 issue #1 亦走了其评审格，回执镜像 /tmp/clean-chrome-issue1-review.md）：三件全 CONFIRM。件一判据同向，对面收紧为「充分三支加必要性证明」，并指出 S002 的 CDP 覆写差分支撑格现状为空，按需立探针补实测不拿推断当必要性。件二 S008 矩阵 pipe/both 格归对面，证据两级互补：pipe 乘 browse spawn 形参格用我方 e2e 断言输出，both 乘直连/flat 格由对面 Windows 原生跑 pipe-smoke.py 实录（其默认 --exe 指旧路径须显式传参，对面评审 G7）；我方 README 注记不候 both 格。件三优先序：item 4 立即在 152-r2 做（纯验收零引擎改动），items 1/2/3/5 挂 155 窗（对面 REQ-012）独立 REQ，前置与我 #27 同句（token/字节与注入痕迹对比数据）；衔接面约定：引擎版到位后 CLI 退化 Browse.* 直通方言 session 域，同方法名同参数面不做第二套，方法面唯一真相是对面 REQ 冻结版、我方落地显式跨仓引用；gating 走 base::FeatureList 默认关（上游 kDevToolsWebMCPSupport 先例），未开得 -32601 时 daemon 回退 stock 路径即我方近端已落那套，不废弃。对面 F3 实锚我仓三处（record.rs:59 startScreencast 帧流、js_host.rs:438/717/738 __browse_ref_gen），我方复核全中，据此立台账 #30（近端瘦身消注入痕，title/url 改 Target.getTargetInfo）。[实证: 我方 grep 三锚复核；对面引文归属勘误（REQ-009/S006/ADR-0005 系其仓编号）双方对齐]
- 批 1 开工（#29）：e2e `exercise()` 补 `navigator.webdriver` 恒 false 断言（通道参数化，port 与 pipe 各验一格），README 版本说明注记回填（删「pipe 态未验」，改 e2e 断言在册）。批 diff 走评审格两轮快核：F 二处修（diary 权威句改指针式；CHANGELOG 追正口径收窄为 port 与 pipe 两通道，both 态属 clean-chrome 侧矩阵，本仓 spawn 面本就二选一）加 G 五条采纳；增强格留档：Emulation.setAutomationOverride(true) 下仍断 false（S008 显式开关场景），后续测试增强批再接。[实证: BROWSE_E2E=1 BROWSE_NO_ATTACH=1 cargo test -p browse-core --test e2e 双通道 35.58s 全绿；fmt、clippy、test、doc 四门禁绿；两轮快核 CONFIRM 放行]

- 批 2 开工并交付（#21(ii)，patch）：`preview()` 类型化短预览（布尔/数字/字符串带引号/容器 JSON 截断，报错与 `print()` 受益）加 `render_result()` 字符串带引号（serde 序列化形态，数字布尔仍裸打、空容器仍不打）加大值落盘双轨（文件写原文，stdout 才带展示引号）；方言错误契约补字符串 receiver 用例。快核四边界 2 CONFIRM 加 2 F：`__dropped` 回执 bytes 从展示长度改为工件字节（raw.len()，消两口径），getting-started 输出样例补引号；评审方预授权单行修免再轮。G 采纳：bytes 语义入 pub 文档加 doctest 回归锁（DROP_THRESHOLD+1 字符串落盘，回执 bytes 严格等于工件长度）。行为变化自觉申报过闸：CLI 字符串结果输出从裸改变带引号。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 全绿；评审方实跑二进制四形态（字符串/JSON 文本串/容器/浮点）验证]

- 批 3 开工并交付（#21(i)，minor）：JSON 命名空间（parse 带坏 JSON CTA、stringify(v, indent?) 两格缩进多行）加值方法面（字符串八法、数组四法，slice 按 UTF-16 单元与 .length 同口径）加类型感知 CTA（STRING_METHODS/ARRAY_METHODS consts 派生，method_face_catalog_lists_all_methods 把 consts 与 surface 目录描述绑死，增删只改一边即红）。快核两轮：一轮 F（includes 数值等值按 serde 形态，[1].includes(1.0) 假阴）修为 json_value_eq 宽等（i64/u64 精确段先比浮点兜底，评审方逐段推演无洞）；二轮新 F（缓办无载体）补本条留痕：哨兵卫生（__host=json/session 哨兵可随变量泄漏与可伪造命中，系 session 既有先例的放大）缓办，归属 #21 后续或 #30 近端批裁。G 全收：四项 JS 偏差进 surface 条目（agent 可查面）、parse_number 补分型 CTA（超范围给 BigInt 出路、裸负号归因形态、1.2.3 有锁）。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 全绿；llms 92/120 帽内；评审方隔离实例实跑探针后下线]

- 批 4 开工并交付（#18，minor）：反引号模板字符串 raw 语义（只有反引号转义与 \${ 两转义，其余反斜杠序列逐字保留，真换行合法）加普通字符串丢字修（\n\t 引号反斜杠照常，其余保留反斜杠，\d 不再静默吞）加双追踪器同步（strip_comments 与 depth_ok 认反引号）。两轮快核：一轮 F（追踪器奇偶坑，评审方实弹复现）修为 scan_raw_close 单一真相（连续反斜杠奇偶判定，解析器与两追踪器三处共用）；途中我方 post-process 再犯同类奇偶错被矩阵用例抓出，改 run 奇偶结算。用户令严格测试四层齐：单元（parser_contract 14 例：闭合奇偶矩阵 0 至 5、转义全表、\$ 矩阵）、集成（模板与 JSON 与值方法面组合流）、验收（e2e 双通道真 V8 过 #18 原文四用例）、A/B（同载荷模板形与旧式手工转义形同值等价锁；首版旧式编码带 shell 直觉被实弹抓红：方言双引号串里反引号本是普通字符）。二轮 CONFIRM 终审放行；评审方复验口径沉淀：先 build 再 CLI 探针，防二进制滞后假红假绿。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 全绿；e2e 双通道 34.85s；评审方重建二进制后逐格实弹对账含奇偶 0 至 5 与复现件]

- 跨仓后续两笔（clean-chrome 工位，2026-09-19 下午）：件二 pipe 补验 Windows 格绿（spawn 直连加生产形参，pipe 态 CDP 往返加 flat 会话 webdriver false 加 9222 不听，both 态双通道加 9222 听；S008 Windows 格其仓已回填，lan 两端 pipe 格候 POSIX 臂 155 窗），我方 e2e 断言输出与其 pipe-smoke 实录（带恒 false 断言加 --no-sandbox 透传，其 commit 03ee43a）双轨互补在案。部署面课题移交：部署位 exe 遭沙箱受限令牌拒访（sandbox_win.cc:804 报 0x5 后 DCHECK FATAL），纯形必崩而生产 spawn 恒带 --no-sandbox 免疫；修复第三案实证 icacls AppContainer ACE（*S-1-15-2-2:(OI)(CI)RX /T）一行收（部署位 r2 纯形加沙箱即活），据此立台账 #32（chrome install 落位补 ACE 加 doctor 检测），三案全谱在其仓 S008 五节。[实证: 对面 schtasks 脱管跑双态全绿；9339 监听实测]

- 批 5 开工并交付（#20 加 #19，minor）：waitForResponse(pattern, ms?)（glob 同 route 族；窗口语义最近命中含历史，前提 Network 域先开，方言无并发可用形态是触发后等待；体等 loadingFinished 再取，体窗 5 秒，失败显式 bodyError）加 responseBody(requestId)（过期 rid 指引进错误链）；#19 换靶同步开域五入口全覆盖（use 臂、new_tab、switch_tab、engine 的 attach_first_page 与 new_tab；防漏口径与高级路径豁免注记写进 ensure_page_enabled 文档），cdp 层留纯策略不上收。两轮快核各出一真 F：body 竞态（命中响应头即取体被静默吞，评审方真慢体 server 复现）修为体完成信号等待；engine 主路径漏挂（评审方全新 daemon 首片段假超时复现）修两行。我方途中自查一个真 bug：事件字段嵌 params.response.*，首版指针漏 params 段致超时，多层探针定位；另踩 pkill -f 自匹配杀自己 shell、e2e 中断残留引擎占 profile 锁两坑。回归锁形态：真网慢体受 WSL 到 Windows 引擎回环边界限制（评审方指正：WSL 侧 Linux r2 产物连 WSL 监听是通的，真网慢体 e2e 日后可在 WSL 用 Linux 产物补格），主锁为 connect_pipes 内存管道假 CDP 对端单测（先拒 No resource 后放体 400ms 时序，另锁永不完体的 bodyError 路径），BODY_WAIT_MS 测试态 800 门控。cdp wait_for 超时补 CTA。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 绿；e2e 双通道 34.99s；评审方真引擎加真慢体 server 实弹复验 F1F2，五入口 grep 对账]

- 日终全量 code review（任务协议：委派即产物，收件人模式；范围 c2eb767 到 5a277f7 六笔整文件集，评审方静态全文读加隔离实例实弹）：5 F 加 6 G 加 6 无发现面加 10 测试盲格，产物 /tmp/browse-rs-day-fullreview-2026-09-19.md（评审方临时件，结论以台账 #33 为载体）。核心洞察是跨批组合死角：#19 逐 session 开域乘 use_target 只 attach 不 detach，多 tab 流程后陈旧 session 三倍重复投递事件（实测 300 请求页 168 条 URL 仅 56 唯一），放大 waitForResponse 跨 tab 误命中与超时 CTA 体积；另捕数字字面量指数下划线静默截断（return 1e5 得 1）、preview 大值全量序列化尖峰。我方五锚复核全中零翻案，立台账 #33（修序 F1 打头 F2F3 同处落地，六笔不回滚前进式）。无发现面六组入册（模板与转义三处口径、普通串转义回归面、渲染与落盘三件、体就绪路径、#19 五入口、cdp 守卫与事件族加投影）。[实证: 评审方私有 daemon 加 Linux r2 产物加本地慢体与多请求 server 实弹，核完清场；use_target 全仓零 detachFromTarget 我方 grep 复核]

- 批 6 开工并交付（#33 修复批，全量评审 5F 加 6G 全收）：F1 use_target 换靶即 detach 旧附（事件三倍重复根除，e2e 锁换靶全走过后一次导航恰一份）；F2 waitForResponse 按活动 session 过滤（管道单测锁他域短窗超时）；F3 超时 CTA 封顶（活动窗计数加去重加最近三条）；F4 指数下划线数字字面量即报（不再静默截断）；F5 preview 有界序列化（头部 8 项加共 N 项尾注，途中自查双省略号撞车与对象括号错配）；G 六条全落（GLOBALS_CTA const 加双向契约测试、responseBody 与 waitForResponse 同形、先剥注释配平、不 trim 保行号、深等披露、CTA 指引）。快核出新 F 一条（本批自引回归：detach 撕断录制帧流且停场落错 session），修为 pin 机制加定向停：cdp Session 纯机制 pin_session/unpin_session，recordStart 钉住录制 session 跨换靶存活，recordStop 定向对本体停加 sessionChanged 回执告警，e2e 实弹锁帧流存活。二轮 CONFIRM 终审放行。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 绿；e2e 双通道 35.57s；评审方独立实弹复验 pin 与对象预览零翻案]
- 批 6 遗留三项（评审二轮新 G，低危）：pin 无超时回收（录制忘收场则钉住 session 持续投递，候兜底解钉或启动新录制时清理）；waitFor/peekEvents/findEvents 无活动 session 过滤（多 tab 或钉住期间可能消费非活动事件，候同口径过滤或 surface 注明）；对象尾注断言已随批补，前两项进下批裁量。

- 批 7 开工并交付（薄封装批 #23 加 #24 加 #25.2 加 #25.3，minor）：交互动词（pressKey 组合键 vkCode 补齐、hoverRef/hoverAt、dblclickRef、dragRef 鼠标序列、keydown/keyup、typeRef 真实按键序列）；截图选项（format jpeg/quality、ifChanged 字节级跳过）与 emulate 视口 UA 仿真；setInitScript 每新文档注入；storageState 往返（Storage.getCookies 加 DOMStorage）。两轮快核：F2 处（initScript 替换叠加与清除无效，修为 sessionId 到 identifier 集合记账加 drain 全撤，即时路径与 watcher 双注册是实雷，实弹三态 [null,1]/[null,null] 验收；screenshot 条目漏更）加 G 五条全收；G5 落地时实弹再捕真 bug（getDOMStorageItems 回包 {entries:[...]} 原解析按顶层数组恒空，同块对照暴露）。跨端口径（用户令）：新面零 cfg(windows)，vkCode 是 CDP 协议字段名跨平台通用，矩阵有头端验收列为批后置项。e2e 验收块：hover 显形、dblclick、Control+a 全选覆盖、typeRef 入 contenteditable（需 role=textbox 进 AX 树）、initScript 三段锁、storageState 双半边往返、ifChanged 二次 skipped、jpeg 后缀、emulate innerWidth。途中实弹坑：data URL 的 # 被 fragment 截断（class 替）、AX 树 StaticText 节点 focus 无效、/tmp 残留假 skipped、组合键带 text 被当字面输入。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 绿；e2e 双通道 41.97s；评审方复跑复现件三态全过零翻案]
- 过程教训（批 7 三处中招，评审方同判）：脚本化字符串替换对 fmt 后代码形态屡次哑火且不报错，靠行为复现才察觉；后续文案与代码改动固定跑「契约测试加关键短语 grep」双检查，已在本批后段执行。遗留（评审 G-lite）：initScript 替换只对活动 session 重注（非活动已附着 session 旧撤新不补，与条目口径不冲突）；init_script_ids 留已 detach sid 条目（下次 remove 失败忽略，无害）。

- 批 8 开工并交付（生命周期线 #25.1 加 #25.5 加 --isolated，minor）：daemon 闲置回收机（BROWSE_IDLE_TIMEOUT 缺省 1 小时 0 关，退引擎不退 daemon，CLI --idle-timeout 随新 daemon 环境注入）；代理直通（EngineSpec 三新字段到 spawn_extra_args 派生 --proxy-server/--proxy-bypass-list，port/pipes 四臂 extra_args，BROWSE_PROXY 同值）；--isolated 隔离态 profile（spawn 落 state/isolated-时间戳，退场即删，清理失败留痕）。三轮快核：一轮 2F（回收机不取单飞槽会撕断在跑求值且 Not connected 兜底重放整段；env 与旗标两态使 BROWSE_PROXY/PROFILE 一会生效一会不生效且求值路径静默吞三旗标）修为单飞槽加双确认、up 缺字段回落 from_env、run_eval 守卫透传；二轮 1F 为我方修复自引（unreachable 在 BROWSE_CDP_WS 在册配置下 panic 撕断请求）修为 env Attach 直通加四元组解构；三轮 CONFIRM 终审放行。实弹验收：idle 三态（Spawned 到 6 秒窗 NotConnected 到 eval 回拉）、长求值不撕不重放、代理旗标入 cmdline、isolated down 后零残留、BROWSE_CDP_WS 下 up 语义错误不 panic。G 全收：isolated 清理留痕、spawn_extra_args 纪律注释（单 flag 直通不转义不拆分）、--isolated 优先级注明、idleMs 用 still_idle、engine_up 双 browse 前缀去重。单测 spawn_extra_args 形锁、e2e 新测 isolated_profile_removed_on_shutdown。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 绿；e2e 三测 44.12s；评审方四边界逐条实弹复验零翻案]

- 过程教训（批 8）：我方两次踩「二进制滞后源码」口径（探针前忘重建 CLI），评审方口径三次验证必要；脚本化替换哑火在批 8 再现一次（isolated 目录块未落），行为复现加 grep 双检查已成本仓纪律。

- 批 9 开工并交付（凭据线 #25.4 --secrets，minor）：dotenv 密钥仓（load_secrets 进程级全局，注释空行忽略、配对引号剥离、重复键后盖、坏行带行号）加方言 secrets.<NAME> 命名空间（Ident 哨兵加 Member 查表，缺键 CTA）加渲染面脱敏（mask_secrets 值含密钥即整值换 ***，嵌套容器递归；防切片还原取保守全换）加 CLI --secrets 旗标（随新 daemon 环境注入，两仓口径：取值看 daemon 启动时仓、展示看本次 CLI 旗标）。三轮快核：一轮 F2 处（F1 CLI 进程零脱敏，return secrets.TOKEN 明文上屏，根因是渲染发生在 CLI 侧而只有 daemon 载仓，修为求值与 up 两路 CLI 都 load_secrets 且旗标优先回落 BROWSE_SECRETS；F2 落盘工件被面具污染，修为字符串落原文、容器落未脱敏 serde 序列化）加 G（BOM 剥离、mask_secrets_str 子串脱敏保 CTA、surface 口径句）；二轮 G4 只落半边（helper 与单测在、err_response 无调用点，守卫错误链仍漏明文，首版接线被脚本化替换哑火吞掉，grep 只余定义与自测两处），修为错误链先过 mask_secrets_str 再上前缀，顺带 CLI run_snip 错误臂同过（兜 context 层），测试从定义处升级为调用点锁（err_response_masks_secret_values 复刻守卫文案嵌密钥形态，断言无明文、含星、CTA 保留、前缀不回归；SECRETS_TEST_LOCK 从 js_host tests 提为 crate 级 cfg(test) pub(crate)，三测试共用串行防全局互踩）；三轮 CONFIRM 终审放行，评审方加对照探针（不带 --secrets 的同字面量仍明文）证面具按已载密钥命中非无条件改写，与 G3 口径句一致。std Mutex 持锁递归自死锁（嵌套容器实弹雷）修为先取判定放锁再递归；SECRETS 全局并行测试污染修为 tokio Mutex 串行。G2（export 前缀、行内注释）记下批裁量。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 绿；评审方三轮实弹（明文复现件反转、40KB 工件 JSON 合法且 bytes 对账、BOM 首键、守卫错误链脱敏、无旗标对照）零翻案]

- 批 10 开工并交付（#30 近端瘦身加 #34 随批修，minor）：snapshot 链路消主世界注入痕（window.__browse_ref_gen 全撤，代际改 daemon 侧文档代计数：cdp doc_gens 按 sessionId 记账，主框架 frameNavigated 事件递增加 Page.navigate 回执同步递增双源，判据只需不等；iframe 与同文档跳转不递增保 SPA 语义）加 url/title 改 CDP 查询面（主源 Target.getTargetInfo，回落 Page.getNavigationHistory，snapshot 零 evaluate）加 lookup_ref 校验 daemon 侧化（活动 session 相等加文档代相等双判，换 tab 后代同值也不可信）。两轮快核：一轮 F1（architecture.md 投影漂移仍写旧标记）加 G 四条（重试面收敛、预算辨识、None 口径、同步计数直测），修为 call_commit_retry helper 两出口同用（screenshot 与 snapshot AX 拉取）、耗尽错误带归因 context、文档写准 None 按过期、单测补回执即加代断言；二轮 F（缓办项声称记档无载体，diary 顺序错在回执之后）加引用失真纠正（评审方探针的旧 title 本是正确值，不作提交窗读旧值实锚），补 diary 载体与重试语义单测（两次瞬时拒后成功加非瞬时立刻上抛）加 e2e 提交窗元信息一致性格。途中真发现一件：#34 同片段 navigate 后紧接 screenshot 稳定报 Not attached to an active page（data: 与 https 均 3/3），系 HEAD 既有产品面竞速（navigate 回执先于提交完成），#30 的同步计数把 e2e 时序塌缩进提交窗才显形（stash 二分定位加单发复现证非 #30 回归），立项 #34 随批修。遗留三件下批裁量：根因级 host 统一等提交（navigate 后首个动作等一次 frameNavigated，替代出口级重试）；doc_gens 随 sessionId 累积不清理（daemon 常驻生命期，量级小）；提交窗内 getTargetInfo 疑似读旧 title（未证，e2e 一致性格已可观测，红了即提前根因级）。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 绿；e2e 双通道加 isolated 3 测 44.14s；评审方真引擎八探针（零注入、url/title 源、真导航失效、pushState 存活、竞速三连、同族旁证）零翻案]


- 批 11 开工并交付（#34 根因级加批 10 三遗留清账，patch）：host 统一等提交屏障（cdp Session commit_barrier：跨文档 navigate（回执带 loaderId、无 errorText、非下载）取 send 前事件 seq 为水位，后续页面级 call() 闸到水位后的主框架 frameNavigated 才放行，25ms 轮询、生产窗 5 秒、超时保守放行留痕 daemon.log；call_on 不闸保 watcher 应答；出口级 call_commit_retry 降为兜底）加 detach 清账（use_target 换靶随删旧 sid 的 doc_gens 与屏障水位，重附新 sid 代从 0 起）加 (c) 随屏障消（snapshot 的 AX 拉取过屏后 page_meta 读到提交后世界，e2e 一致性格在岗）。两轮快核：一轮 F 高危（屏障用 peek 取最旧 50 条，长会话攒过 50 次主框架导航后新 commit 永久出窗，每导航白等满窗，评审方灌 60 次实测 0.06s 到 5.02s 劣化）修为 peek_events_since 游标取，回归锁预灌 60 条旧事件断言 300ms commit 即放行；G 三条（超时留痕、isDownload 排除、覆盖面写准）全落。二轮 CONFIRM，评审方复刻件逐字重跑反转（60 次导航后 0.07s）。自觉披露两坑：水位必须 send 前取（响应续体晚于 commit 事件被调度则 send 后取会把已到 commit 盖水位下致永假等）；BARRIER_WAIT_MS 的 cfg(test) 跨 crate 不生效（首版超时测试放 browse-core 实测拿到生产 5000ms 常量红，迁 cdp 本 crate 才生效，测试常量跨 crate 是新坑入册）。实弹：同片段 navigate 后 evaluate 三连确定性见新文档（f1/f2/f3 各自命中，比不报错更强的正确性主张）、data 带锚点导航 41ms 零感。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 绿；e2e 双通道加 isolated 3 测 44.14s；评审方真引擎 A/B 实测（新 daemon 0.06s 对灌 60 次 5.02s）零翻案]


- 批 12 开工并交付（小件池：批 6 遗留两件加批 9 G2，patch）：waitFor 活动过滤（只认活动 tab 与 browser 级事件，他 sid 不被误领误消费，取出即移除的破坏性面堵住；peek 族维持全缓冲不过滤为检视原语，surface 与超时 CTA 写明分工）加 pin 回收兜底（reset_connection_state 在两连接落成点清 pinned/doc_gens/屏障，引擎换代即自动解钉，own_targets 是 browser 级不清）加 dotenv G2（export 前缀剥含制表符与多空格且 exporter= 不误伤；行内注释未引值从「空格井号」截断、引号内井号字面）。一轮即 CONFIRM（评审方双 tab 实弹复现过滤生效、.length 间接验值避面具遮蔽）；G1 测试补写时自己抓到真缺陷：旧连接读泵迟到退出翻共享 connected 旗误杀新连接（单跑绿全套红的形态），修为 conn_epoch 连接纪元守卫（退出与投递都判纪元，被取代的泵静默），追加回执二轮 CONFIRM。G3（未闭合引号宽松面）按评审裁定留档不立错。[实证: fmt、clippy、12 组 test（cdp lib 16 三连绿）、doc test、aidoc strict、PEVO 绿；e2e 双通道加 isolated 3 测 44.17s；实弹共用 shell 文件三键全 *** 加 switchTab 后 waitFor 短窗超时带分工句]

- 第二日全量 code review（任务协议同上次；范围 962d6cf 到 65475c8 六笔整文件集即批 7 至批 12，23 文件 +3374/-152，评审方静态全文读加隔离实例实弹）：F 1 加 G 4 加无发现面 6 组加盲格 7 项，产物 /tmp/browse-rs-day2-fullreview-2026-09-19.md（评审方临时件，结论以台账 #35 为载体）。增量价值集中在两处跨批状态机：连接生命期与在途请求的对账（F1：断连不清 pending，硬等 30 秒并占 eval 单飞槽，错误把「连接已死」报成「超时」归因反）；「重连清账」口径只落 cdp 侧三表、js_host 侧 init 记账没跟上（G1）。无发现面六组：waitFor 活动过滤乘既有调用方（双 tab 不领不消费）、屏障乘 idle 回收无互锁、spawn_extra_args 注入面闭合、storageState 参数面、secrets 面具乘落盘双轨、doc_gens/屏障乘 pin。我方三锚复核全中零翻案。[实证: 评审方实弹复现件（永不回应的 promise 加 18 秒杀引擎，START/END 跨度 32s 实测）]

- 批 13 开工并交付（#35 修复批，F1 加 G1 至 G4 全收）：F1 三处落（两臂泵退出 epoch 相等分支 drain pending，每个在途 oneshot 回 error 形响应走 CDP 错误分支（带方法名与「连接已断开」归因，比 drop sender 的 cdp dropped 可诊断）；reset_connection_state 同 drain，锁序 outgoing 先于 pending 且 send_with 改先占 outgoing 再登记消灭「已登记却被清」窗口；send_with 超时分支补 pending.remove 防 map 积尸，迟到响应由 route 的 pending-miss 静默丢）。回归锁 connection_death_fails_inflight_calls（假对端不回应加 200ms 断连，断言 2 秒内失败含归因）。实弹复刻评审方复现件按 PID 精杀引擎：8 秒即时失败（修前 32s），首跑 31s 未反转是我 pkill 模式没中引擎没死（活连接普通超时），复验同坑注意。G1 落 watcher 纪元观察（pub connection_epoch，换代清 init_script_ids 与 enabled 表）；G2 emulate 条目补 UA-CH 半句；G3 architecture.md cdp 段补状态机摘要；G4 三处时序上界放宽 2.5s 下界不动。盲格 #1 #2 #3 前半随批落锁，#3 后半与 #4 至 #7 记档下批裁量。一轮 CONFIRM。[实证: fmt、clippy、12 组 test（cdp lib 17 三连绿）、doc test、surface 重生成、aidoc strict、PEVO 绿；e2e 3 测 44.16s；评审方独立实弹（CALL_END 与 KILL_AT 同秒、错误串归因正确）零翻案]

- 批 14 开工并交付（#32 部署位补 AppContainer ACE，patch，纯 Windows 面增强）：chrome_mgr 新增 apply（icacls grant 单 argv 元素 *S-1-15-2-2:(OI)(CI)RX /T，非致命留痕）与 detect（icacls findsid 按 SID 匹配，输出含传入路径串即在位；实测 findsid 无匹配退出码同 0 只能靠路径判阴阳，显示名本地化故全程 SID 串）；装机两源（本地导入复制后、镜像 rename 后）接线，回执 appContainerAce（Windows true/false 他平台 null）；doctor 逐版本增检测、healthy 纳入 ACE 与 filesMatch（评审 G1：filesMatch 此前漏判，评审机 r2 的 569/567 漂移是活证）、缺则 hints 出 icacls 修法 CTA 与漂移 CTA（G-lite：不许 healthy=false 加 hints=[] 哑组合）；ADR-0003 Context 补根因段（0x5 是 AppContainer 无权读部署位，第三案 ACE，spawn 决策不变，规格与 Program Files 默认 ACL 同）；surface 两条随卷。Windows 实弹（interop：源树 rsync 到 C: 加 cargo.exe 构建 38.5s）：本地导入装 r2ace 回执 true；doctor 双版本（既有 r2 手工补加新装自动补）true 且 healthy；纯形验收净目录 exit=0 带 DOM；A/B 反证剥 ACE 后 0x80000003（正是 ADR-0003 记录的 FATAL 形）复补恢复；pin 切换与 remove 守卫顺带验证；清场全净。两轮 CONFIRM（一轮即过加尾巴轮收口 G-lite）。Python heredoc 转义又把反斜杠写成引号（\ 到 "），Edit 工具正修，双检查纪律生效。遗留：G5（chrome use 回执缺 ACE 提示）、Windows 验收脚本化（载体 pwsh 遵环境口径）、检测判据的路径形态耦合回访面，均下批裁量。[实证: fmt、clippy、12 组 test、doc test、aidoc strict、PEVO 绿；Linux 单测扩格（非 Windows null 三断言加漂移 hint 断言）；评审方 Linux doctor 复跑同形态对上]

- 批 15 开工并交付（#22 全量 JS 受限旁路，minor，用户裁定「开受限旁路」）：ADR-0002 Decision 增修订段（当年否的子命令操作面仍否着，只开语言旁路；分工口径方言管 CDP 编排、--js 与 pageEval 管页面逻辑）加 pub eval_js（Runtime.evaluate 直发 returnByValue 加 awaitPromise 双开，页内抛错带描述加 CTA；return 桥先原样发、SyntaxError 认 Illegal return 后包 async IIFE 换行定界重发一次）加方言全局 pageEval(js)（GLOBALS_CTA 双向锁随卷）加 /eval js 旗标分流（自由 async fn，闭包装箱与生命周期缠的教训）加 CLI --js（穿 run_eval/run_snip/run_stdin；空实参加管道 = 整段 stdin 一次求值，JS 语句边界不由括号配平决定）加 --b64 通道（实参与管道整段两形，字母表先行校验要错不要垃圾；-b 短参是 issue new --body 既有契约不夺权只留长参）。三轮快核：一轮 F 三洞（return 不在句首、包装体非 async 致 return await 非法、同行拼接被行尾注释吞闭合括号，评审方实弹三发全中）修为重试制桥；二轮 G 两层（不可序列化静默空；我实弹再揭真 Chrome 在 returnByValue 下节点序列化成 value:{} 无 subtype，与空对象协议层不可区分）定谳为删硬判据臂、披露句写全（Date/Map/RegExp/容器内元素同形，CLI 零输出口径，CTA 在手册句）；三轮尾巴（假对端锁了引擎不产的形状即语义空转）改真机形状断言。实弹八通道全通（--js 直引 return 形与表达式形、管道、--b64 实参与管道、方言 --b64、pageEval、方言管道乘 pageEval 跨语言组合）。过程坑三件入册：python 双开句把 surface.rs 截断成空文件（open 写截断后才 read，git 恢复加重放，脚本化替换哑火家族新变体）；python 转义两次写歪（四反斜杠、format 不配对大括号）；daemon 侧二进制滞后（CLI 重建不等于 daemon 换代，复验口径补 down 加 up）。V8 全局词法环境跨求值复用（两次 const 同名撞）入 e2e 注记。[实证: fmt、clippy、12 组 test、doc test 3 组、surface 重生成、aidoc strict、PEVO 绿；e2e 3 测 44.33s；评审方三洞实弹与 Date/Map/RegExp/容器内元素四形实测零翻案]
裁定：issue 定性的权威载体是 issue 台账与后续各批封版 REQ，本篇只记当轮指针与过程；上文批次表里的 patch/minor 是预报，以封版 REQ 定稿为准。后续各批交付走评审闸门，先闸后推；S008 矩阵 pipe/both 格已定归 clean-chrome 工位回填（pipe 格采我方 e2e 断言输出，both 格其 Windows 实录）；对面 issue #1 判据修订与吸收候其用户裁定，届时跨仓引用回我仓终审与 #27/#28/#29/#30 编号。批 2 的 stdout 形态变化封版时 CHANGELOG 记行（评审方 G，封版流程内）。

- 批 16 开工并交付（issue 台账 Roadmap 批 1 = REQ-006：#51 秒口径加 #19 goto 加 #39 历史导航与小动词，minor）：#51 wait 族 timeout 统一秒口径（waitLoad/waitIdle/waitFor/waitForResponse/waitJs 五面，缺省 10/10/15/15/10 秒；混用守卫 secs_to_ms 大于 3600 按毫秒误写换算加告警加封顶 600 秒，旧毫秒习惯值等价迁移零破坏；对象面回执附 timeoutWarning，事件/值面走 daemon 留痕；错误文案秒化；BROWSE_IDLE_TIMEOUT 仍毫秒系 env 配置面明示不动用户配置）；#19 goto(url,{timeout 秒,waitIdle}) 一步导航（navigate 加 waitLoad 加可选 waitIdle，回提交后世界 url/title/elapsedMs；errorText 显式归因）加 clickRef {waitNav,timeout}（同文档锚点立即返回不误等）加 waitFor 词条竞速警示（loadEventFired 注册晚即假超时，指路 goto/waitLoad/frameNavigated）加 getting-started 首例改 goto 范式；#39 goBack/goForward(delta 钳界) 加 reload({ignoreCache}) 加 checkRef/uncheckRef（callFunctionOn 读实态不一致才点，幂等，radio 已选不反点，非 checkbox/radio 报错指 clickRef）加 fillRef/fillInput 第三参 submit（顺带 Enter，返回保持回读串契约不改形）。坑三件：秒口径把 e2e 旧毫秒实参放大（waitForResponse 负例 200 变 200 秒挂死，全套 e2e wait 实参迁秒口径 15000 至 15 等 19 处）；fill 返回是回读串非对象，submit 标记附不上定谳为纯副作用不改契约；pkill -f 模式串出现在自家命令行自杀整条 bash（括号技巧 engine-profil[e] 规避）。守卫单测三件（边界 3600/3601、封顶 600、submit 两形）。[实证: fmt、clippy、13 组 test（lib 53 加守卫 3 新）、doc test、surface 重生成六新条目随卷、aidoc strict、PEVO 绿；e2e 3 测 46.24s 双通道加 isolated；goto/goBack/goForward/reload/checkRef 幂等/uncheck/submit 副作用/秒直写零告警/毫秒误写带告警九断言全中]

- 批 16 评审一轮修复（F3 加 G 全收）：F1 history_jump 三兜底重写（空历史先 bail；currentIndex 缺失或越界 bail 不再冒认最早条；entryId 缺失 bail 不用索引顶替不透明整数）；F2 clickRef waitNav 显式构造对象形回执（click_ref 基座是裸 true 布尔面附不上键，特性曾端到端不可见）补 e2e 断言；F3 issue list 非 2xx 读响应体 error 归因（4xx 指核参数，5xx 才稍后重试）；G1 waitFor surface 条目漏迁秒化；G2 五词条补 1000 至 3600 按秒直解的放大窗披露；G3 waitFor 两参旧形容忍恢复；G4 timeout 实参非整数秒报错不落缺省加单测三态，goto 与 waitNav 的 opts.timeout 同律；G5 wait_load 先查 readyState 删 frameNavigated 宽限窗（已加载页真立即返回，修前 goto 默认预算白等 5 秒）；G6 REQ 登记面三处对齐；G7 goto 记 url/title 元数据来源与权威读取口径；submit 词条补对话框指路。BROWSE_IDLE_TIMEOUT 维持毫秒（评审 CONFIRM：与页面 wait 族不同概念）。过程坑：md-guard 裸跑是 hook 模式等 stdin 挂死，正道是 --staged 检暂存区。[实证: fmt、clippy、13 组 test（lib 54）、doc test、surface 重生成、aidoc strict、PEVO 绿；e2e 3 测 46.69s 含 waitNav 新断言]

- 批 17 开工并交付（#57 日终全量评审修复批，browse-codex-review 首单四 F 加六 G）：F1 秒口径误写判据从大于 3600 收到不小于 1000（1000 至 3600 静默秒直解窗实弹炸在本仓测试：waitForResponse 300 意图变 300 秒，是 lib 套件 301 秒的全部来源）加 waitForResponse 超时文案秒化（全族最后一处毫秒残留）；F2 测试残留旧毫秒实参三处迁秒（300 至 1、3000 至 3、1000 至 1）加 e2e 示范值 8000 至 8，套件 301 秒回到 3 秒；F3 doc_gens 自增搬进 cross_doc 块（fragment 导航回执无 loaderId 不再假换代白拒有效 ref，评审真 chrome 实弹 g2 到 g3 定谳）加 cdp 回归锁（假对端旗标切 loaderId 阴阳）；F4 Session close 补 drain（conn_epoch 递增加 reset_connection_state 同律，在途调用 2 秒内失败不再白等 30 秒占 eval 单飞槽）加 cdp 回归锁；G1 屏障弱保证入册 architecture（水位换靶窗与入口不过滤两方向都偏保守）；G2 detach 失败按错误码分流（-32602 与 No session 静默，其他升级留痕指 #33 形）；G3 newTab 内部预算 8 秒对齐 goto 缺省 15 秒并入 surface 词条；G4 封顶语义写准（秒直写上界 999，误写换算封顶 600）；G6 issue new 加 --dry-run（同规校验零网络副作用预览载荷，实弹过且台账零新增，教训是评审首单实弹 -b 契约真发 #56 的教训）。盲格六项记档：win 管道乘屏障乘秒口径真机组合归 lan-win 验收面，isolated 加 profile 同给、录制乘换靶乘屏障三方、fetch_by_us 换代残留均未实弹待裁量。评审耗时 1h07m 真实原因附卷：4 并发槽 3 个被 301 秒慢测占，有效并行度 1，F2 修掉后评审也应回到分钟级。[实证: fmt、clippy、13 组 test（lib 54 于 3 秒、cdp 含新二锁）、doc test 3 组、surface 重生成、aidoc strict、PEVO 绿；e2e 3 测 46.11s；dry-run 实弹零落单]

- 批 18 开工并交付（REQ-007 = Roadmap 批 2：#36 限深与 findRefs 加 #37 可观测三件加 #49 detect 七判，minor）：#36 snapshot 参数化（opts.ref 单元素子树部分展开、opts.depth 可见树限深，childIds 随卷输出）加 ax_projected 投影助手（childIds 保留加 parent 边，供限深/子树/祖先链）加 findRefs(q,{insensitive,context}) 服务端检索（name/value 子串匹配，命中加祖先链 grep -C 式，节点带新 ref 可直接 clickRef，引用表整表替换同 snapshot 语义；不引正则依赖）；#37 console({since,minLevel}) 五级分档、jsErrors({since})、requests({since,filter}) 摘要、requestDetail(index 或 requestId) 加 ensure_page_enabled 扩三域（Page 加 Runtime 加 Network 随 tab 入口自动开，晚开域前旧事件收不到入 surface 披露）；#49 detect() 七判加 loading 态（challenged 加 rate-limited 加 blocked 加 stalled 加 login-wall 加 blank 加 loading 加 ok，判序固定，页内探针一次 evaluate 打包加网络计数自事件缓冲，evidence 与 suggestion 随判）。实弹坑三件入册：真 chrome 的 AX nodeId 与 childIds 是数字串（"2"）非数字，as_i64 全数丢弃致 nodes 空，ax_id 归一两形都吃；blank 判据阈值 50 字 30 节点误杀合法小页，收到 15 字 10 节点；daemon 侧二进制滞后复刻（批 15 坑重现：cargo build 前的 clippy 产物不含修复，复验口径 down 加 up 再验）。真页实弹 example.com：snapshot 8 节点、depth2 得 7、findRefs Learn more 命中 2（link 加 StaticText）带可点 ref、requests 200 对账、detect ok。e2e 过程另修两处测试设计：findRefs 断言按 role=button 取命中（按钮与子文本同名命中属正确面）、未捕获异常须 setTimeout 异步抛（同步 throw 被 pageEval 当调用失败返回）。[实证: fmt、clippy -D warnings、12 组 test、doc test 3 组、surface 重生成七条目、aidoc strict、PEVO 绿；e2e 3 测 47.47s 双通道含批 2 块]

- 批 18 评审一轮修复（F2 加 G 全收，F1 真修）：F1 五面活动 tab 过滤（console/jsErrors/requests/requestDetail/detect 的 peek 全接 from_active_session 助手，口径同 wait_for_response 的 #33 F2；评审实弹实锤后台 tab 403 劫持干净页判 blocked 加他 tab console 泄漏，e2e 补跨 tab 泄漏锁双断言）加 F2 requestDetail 收 {since,filter} 与 requests 同解析（index 语义对齐，跨函数直喂不再错位）；G1 login-wall 改密码框独立信号（complete 且有密码框即判，去正文阈值，现代登录页营销文案轻易破 2000 实弹证）；G2 findRefs 零命中早退保留旧引用表并标 kept_refs；G3 console 词条补 assert 归 error 档与未知名落 verbose。过程双坑入册：批 17 的「脚本 abort 丢全部写入」教训本批原样重犯（G1 匹配失败炸在 write 前，F1 修复全丢，e2e 测的未打补丁码（同一坑两次，纪律必须升格为写后 grep 复核断言，本轮已执行）；e2e 跨 tab 块紧邻录制段会致首录零帧（挪到双录制段后全绿，机理未明与 #58 同族待查）。#58 立项（同引擎重复 recordStart 零帧，纯序列可复现，疑 ack 链或 compositor 重启语义）归下批。[实证: fmt、clippy -D warnings、12 组 test、doc test 3 组、surface 重生成、aidoc strict、PEVO 绿；e2e 3 测 48.21s 含跨 tab 泄漏锁]

- #58 线索补记（批 3 前侦察）：同 session 二次 recordStart 零帧是竞态非恒定：同 tab 裸调 Page.startScreencast（带与不带 format:png）均能收帧；二录换新 tab 恒活（r1=4 r2=2）；recordStop 与二次 recordStart 之间插一拍 waitJs（Runtime.evaluate 轮询，每拍过提交屏障）后同 session 也活（r1=6 r2=2）。疑点收敛到提交屏障残留水位与 startScreencast 的闸交互或泵的停止时序竞态，待专项批带屏障日志定位。

- 批 19 开工并交付（REQ-008 批 3 首片：#42 storage 颗粒度 CRUD，minor）：cookies({domain} 域过滤) 加 cookieGet（name 精确匹配 null 即无）加 cookieSet（Network.setCookie，缺 domain 走当前页 URL，path/expires/httpOnly/secure/sameSite 透传）加 cookieDelete（CDP 无单数形，Network.deleteCookies 按 name 加域删全部匹配，守卫 CTA 当场指路过）加 cookiesClear（clearBrowserCookies 整浏览器面，对照 playwright）加 local/session 各 Get/Set/Delete/Clear 八函数（storage_op 统一执行面：键值经 JSON 序列化内嵌防注入，set 回写回读即验）；surface 十三条目与 GLOBALS_CTA 随卷。实弹坑两件：cookie 面需真 http 源（data: URL 被 setCookie 拒 scheme 校验，e2e 改 routeMock 假域起页）；方言片段里比较运算（ev0.length > 0）不可写（值语言），测试侧改两段式取值。#58 线索批前补记（同 session 二录是竞态非恒定，疑屏障残留水位或泵停止时序，待专项批）。[实证: fmt、clippy -D warnings、12 组 test、doc test、surface 重生成、aidoc strict、PEVO 绿；e2e 3 测 47.95s 含 storage 块（中文往返、sessionClear 不动 local、cookie 三态）]

- 批 19 二片交付（REQ-008：#40 a11y 媒质仿真族）：emulateMedia({colorScheme, reducedMotion, forcedColors, prefersContrast, media}) 五参映射 setEmulatedMedia features 加 media 直传（一项不给即报错带五选一 CTA）加 emulateMediaClear()（空参全还原）；与 emulate({viewport,mobile,userAgent}) 并列成族入 surface；e2e 走 matchMedia 双证（dark 加 print 同设 true|true，clear 后回 false）。[实证: fmt、clippy、12 组 test、doc test 3 组、surface 重生成、aidoc strict、PEVO 绿；e2e 3 测 48.46s]

- 批 19 三片交付（REQ-008：#35 鼠标原语补全）：mouseMove/mouseDown/mouseUp/mouseWheel 四原语加 clickAt/clickRef 参数化（opts.button 五键加 clickCount 双击语义，click_ref 主体重构为 click_ref_opts 加薄壳）加 dropFiles（DOM.setFileInputFiles 直灌 multiple，daemon 侧绝对路径）加 hoverAt 复用 mouseMove。实弹定谳一例：dispatchMouseEvent 的 mouseWheel 在导航后首发吞没（渲染器当监听注册握手消耗，三连 w1=0/w2=1/w3=2 实测，mouseMoved 打点救不了），mouseWheel 改走 synthesizeScrollGesture 手势合成（连续 wheel 流首次即触发，e2e 单调用即中）；需要精确单事件的口径入 surface。e2e 五断言：mouseenter 副作用、右键 contextmenu、ref 双击 dblclick、wheel 路径、双文件 FileList 长度 2。坑两件：hover 目标自己 display:none 量出零尺寸（改可见块）；findRefs 式按 name 找节点会命中 StaticText 而非容器（双击目标改真 button）。[实证: fmt、clippy -D warnings、12 组 test、doc test 3 组、surface 重生成七条目加二条目更新、aidoc strict、PEVO 绿；e2e 3 测 49.76s]

- 批 19 四片交付（REQ-008：#38 下载捕获）：ensure_page_enabled 补 Browser.setDownloadBehavior（allow 加状态目录 downloads 子目录加 eventsEnabled，幂等）加 downloads({since})（WillBegin 行情并同 guid 最新 Progress 态与字节；Browser 域事件无 sessionId 不做活动 tab 过滤）加 downloadPath(guid, timeoutS)（等 completed 给 named 路径，改名未落给 guid 原始名候选，canceled 报错，超时带态 CTA）；upload 面由 #35 dropFiles 覆盖（等价 playwright 的 upload/setFileInput，closure 注记）；routeMock 加 opts.headers（Content-Disposition 触发下载的测试通道）。e2e：mock 头触发真下载、downloads 行情、downloadPath 落盘内容回读。坑两件：下载型导航回执是 net::ERR_ABORTED（导航让位下载的正常信号，e2e 改裸调 navigate）；begin 事件与导航回执竞速（垫一拍轮询）。[实证: fmt、clippy -D warnings、12 组 test、doc test 3 组、surface 重生成、aidoc strict、PEVO 绿；e2e 3 测 50.98s]

- 批 19 五片交付（REQ-008 收官：#41 加 #24 残余视觉件）：highlight(ref,{label}) 持久高亮（2px 橙框 pointer-events none 不挡点击，幂等刷新框位，label 叠编号徽标即 annotate 形态）加 highlightClear（按 data-browse-hl 属性清零）加 annotate(refs) 批量画框徽标（ref 本身为徽标文本与 snapshot 编号天然对齐）加 screenshot opts.ref 元素级 clip（scrollIntoView 后量 rect 转 clip 参数）加 opts.hires/scale 高倍采样（clip.scale）；resolve_node_object 升 pub(crate) 供跨模块。e2e：两层高亮（框加徽标）、元素截图有界、annotate 批量加清零。老坑再中一次：按 name 找节点命中 StaticText 而非容器（视觉页 div 改 button 三连了，纪律：e2e 交互目标一律用 button/checkbox 等真控件角色）。REQ-008 五片全落转 implemented。[实证: fmt、clippy -D warnings、12 组 test、doc test 3 组、surface 重生成、aidoc strict、PEVO 绿；e2e 3 测 51.59s]

- 批 20 开工并交付（Roadmap 批 4 首片：#45 /eval HTTP 公开契约，docs 型）：docs/guides/http-api.md 立契约全档（四端点表、/eval 请求三字段与成功失败两响应形、语义要点五条：单飞槽排队不并行、超时 BROWSE_EVAL_TIMEOUT、懒引擎自动拉起、状态跨请求持久、daemon 发现端口口径；兼容承诺只加字段不减字段；curl 三例加与 #44 片段库组合口径）加 --serve surface 词条指路契约档；实弹四例全通（health 探活回完整状态、navigate 加取 title 得 Example Domain、snapshot 限深 7 节点、语法错误回 ok:false 带 CTA）。手册 120 行帽下详细契约住 guide、--llms 经 --serve 词条指路，两全。[实证: surface 重生成、surface_contract 6 绿、aidoc strict、PEVO 绿；curl 四例实弹]

- 批 20 二片交付（Roadmap 批 4：#44 片段库生态升一等公民）：browse snippets list [site] 加 show <rel>（纯文件系统读零协议改动不经 daemon：递归走访状态目录 snippets/，<site>/<task>.js 天然分层，每文件首行 // 注释头当摘要，site 子串过滤目录与路径；空库 CTA 指导存片段）；手册方言节插片段库纪律行（先查库再写新的、注释头格式、执行走 --js 管道或 POST /eval 与 #45 成闭环）顺手纠手册方言例的 loadEventFired 15000 旧范式（改 frameNavigated 秒口径带竞速窗警示，#19/#51 的口径终于追平手册）；surface 两 CLI 条目随卷。实弹：命名实例隔离建库（BROWSE_NAME=snip44 状态目录独立）、两片段分层列读、site 过滤、show 全文。手册 79 至 83 行仍在 120 帽内。[实证: fmt、clippy、12 组 test、surface 重生成、aidoc strict、PEVO 绿；实弹三形（列、滤、读）]

- 批 20 三片交付（Roadmap 批 4 收官：#46 skill 渐进分层）：--llms 手册头部立意图路由表（八行想做什么用什么，细节下钻指路 interaction 层与 HTTP 契约档；手册 83 至 91 行仍在 120 帽内）加 docs/skills/interaction/ 立层并以本日实弹踩坑做种三篇（navigation-race：loadEventFired 竞速窗、fragment 不换代、秒口径、Chrome 禁顶级 data: 跳转；file-download：ERR_ABORTED 正常信号、dropFiles 路径预检、routeMock 头触发下载、begin 竞速；mouse-input：wheel 首发吞没定谳、button 白名单、后台 tab 挂起、findRefs 命中 StaticText 挑 role 纪律）加 docs/README 地图收 skills 层；domain 层与 #44 snippets 目录约定合流不另立。过程坑：route 表两度插错函数（render_help 的 Options 后会掀翻旗标对称断言），终落 render_manual 版本行后。[实证: surface 重生成、surface_contract 6 绿、aidoc strict、PEVO 绿]

- 批 21 开工并交付（#58 重复录制零帧）：record::start 前垫一次廉价会话往返（Runtime.evaluate 1），把同会话 stop 后紧接第二次 startScreencast 的竞态窗口关掉（实证 workaround 同构：插一拍 waitJs 即活 r1=6 加 r2=2；机理未定谳疑 Chrome 侧订阅状态机，注释留线索链）；e2e 补 #58 回归锁（录二场收靶后同会话紧接第三场 recordStart 断至少一帧，修前零帧必现）；实弹三连录同会话第三场 2 帧。[实证: fmt、clippy、12 组 test、surface、aidoc strict、PEVO；e2e 3 测 52.00s 含新锁]

- #59 定谳收口（#58 转化单）：判别实验三连（同 daemon 同页）：背靠背 stop 加 start 必零帧；中间垫 Browser 级往返（getVersion，非会话状态）活；中间纯 sleep 2 秒（零 CDP）也活。结论：stopScreencast 的拆卸是异步时间窗，紧接的 startScreencast 落窗内被一并拆掉；非消息序竞争（若是序，纯 sleep 不该救）。垫拍的毫秒级时延覆盖拆卸窗，修法升级为定谳级。record.rs 注释随卷，e2e 回归锁在岗。[实证: 两判别实验各一次，e2e 3 测全绿]

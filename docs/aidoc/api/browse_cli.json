{
  "crate": "browse-cli",
  "version": "0.7.0",
  "items": [
    {
      "path": "browse_cli::client",
      "kind": "module",
      "docs": "daemon 客户端：本地 HTTP 调用 + 首次使用自动拉起 detached daemon。"
    },
    {
      "path": "browse_cli::client::DEFAULT_PORT",
      "kind": "constant",
      "docs": "daemon 的缺省端口，`BROWSE_PORT` 与 `BROWSE_NAME` 派生端口可覆盖。\n\n取 9880 是为避开 bh 的 9876。"
    },
    {
      "path": "browse_cli::client::UpParams",
      "kind": "struct",
      "docs": "`engine_up` 的参数面：CLI up 旗标直通（字段名即 /engine/up 请求键）。"
    },
    {
      "path": "browse_cli::client::daemon_alive",
      "kind": "function",
      "docs": "探测 daemon 是否在跑：GET /health 通即为在。"
    },
    {
      "path": "browse_cli::client::daemon_bind",
      "kind": "function",
      "docs": "返回 daemon 监听的 `host:port` 串；多实例由 `BROWSE_NAME` 派生端口（ADR-0006）。\n\n# Examples\n\n```\n# // BROWSE_PORT/BROWSE_NAME 均未设时才是默认口（CI 干净环境成立）\nif std::env::var_os(\"BROWSE_PORT\").is_none() && std::env::var_os(\"BROWSE_NAME\").is_none() {\n    assert_eq!(browse_cli::client::daemon_bind(), \"127.0.0.1:9880\");\n}\n```"
    },
    {
      "path": "browse_cli::client::decode_arg_b64",
      "kind": "function",
      "docs": "解码 `-b/--b64` 通道的片段实参（#22）：标准 base64 解码为 UTF-8\n字符串。PowerShell 与复杂 shell 的引号与编码面一并绕开（方言与\n`--js` 两形态通用）。\n\n# Errors\n\n实参不是合法 base64，或解码后不是 UTF-8 文本（错误带生成 base64 的\n可照抄命令 CTA）。\n\n# Examples\n\n```\nassert_eq!(browse_cli::client::decode_arg_b64(\"cmV0dXJuIDE=\").unwrap(), \"return 1\");\n// 空白容忍（PowerShell 折行形）\nassert_eq!(\n    browse_cli::client::decode_arg_b64(\"cmV0\\ndXJuIDE=\").unwrap(),\n    \"return 1\"\n);\nassert!(browse_cli::client::decode_arg_b64(\"!!!\").is_err());\n```"
    },
    {
      "path": "browse_cli::client::engine_up",
      "kind": "function",
      "docs": "POST /engine/up 显式起引擎，走 ensure 全链（附着优先缺则 spawn）。\n\n# Errors\n\nHTTP 失败或引擎 ensure 失败（连不上、chrome 找不到、spawn 超时）。"
    },
    {
      "path": "browse_cli::client::ensure_daemon",
      "kind": "function",
      "docs": "daemon 不在跑时 detached 拉起 `browse --serve` 并探活到通；已在跑则直接返回。\n\n# Errors\n\n拉起失败（exe 找不到）或 10 秒内 /health 不通。"
    },
    {
      "path": "browse_cli::client::ensure_daemon_with_env",
      "kind": "function",
      "docs": "同 [`ensure_daemon`]，但给新拉起的 daemon 进程注入环境变量\n（#25.1/#25.5：idle-timeout 与代理旗标只影响新 daemon；已在跑的\ndaemon 不动，其口径以启动时为准）。\n\n# Errors\n\ndaemon 起不来或就绪探测超时。"
    },
    {
      "path": "browse_cli::client::eval",
      "kind": "function",
      "docs": "把方言片段 POST 到 daemon 的 /eval 求值。\n\n返回最后一条语句的值；daemon 侧错误进 `Err`（错误串已是给人/agent 的下一步指令形态）。\n\n# Errors\n\nHTTP 失败或 daemon 求值失败（语法错、CDP 错、守卫拦截、超时）。"
    },
    {
      "path": "browse_cli::client::health",
      "kind": "function",
      "docs": "GET /health 取 daemon 状态面；daemon 不在时报可照抄的拉起提示。\n\n# Errors\n\ndaemon 不在或响应坏。"
    },
    {
      "path": "browse_cli::client::quit",
      "kind": "function",
      "docs": "POST /quit 退 daemon，daemon 侧顺带只终结自起引擎（附着来源不动）。\n\n# Errors\n\ndaemon 不在（幂等场景调用方先判活）或 HTTP 失败。"
    },
    {
      "path": "browse_cli::client::state_dir",
      "kind": "function",
      "docs": "返回 daemon 日志与运行面目录（`%USERPROFILE%\\.browse-rs[\\<name>]`，多实例各一份）。"
    },
    {
      "path": "browse_cli::issue",
      "kind": "module",
      "docs": "issue 通道客户端（REQ-057 契约，issues.ohmygh.com）：缺陷一键反馈。\n\n提交自动署名 `tool=browse` 加版本（编译期 Cargo 版）加平台加主机名，\n客户端先做与 Worker 同形的校验与截断（title 1 至 200、body 至多 20000、\nversion 40、platform 与 host 64）；读面 list 与 show 走 GET。\n`BROWSE_ISSUES_API` 覆写基址（测与灰度，同 omc 的 OMC_ISSUES_API 惯例）。"
    },
    {
      "path": "browse_cli::issue::ISSUES_API",
      "kind": "constant",
      "docs": "issue 服务缺省基址（Worker 加 D1 真源，REQ-057）。"
    },
    {
      "path": "browse_cli::issue::dry_run",
      "kind": "function",
      "docs": "预览一条 issue 载荷（#57 G6 `--dry-run`）：与 [`new`] 同规校验，不发\n网络请求，回应发载荷（含自动署名字段）。契约实弹与演练走这里，不再\n往生产台账落测试单（评审首单实弹 -b 契约真发了 #56 的教训）。\n\n# Errors\n\n客户端校验不过（title 空或超 200、body 超 20000），与 [`new`] 同文。"
    },
    {
      "path": "browse_cli::issue::list",
      "kind": "function",
      "docs": "列 issue：`GET /api/issues?tool=&status=&limit=&before=`（新到旧，limit 1\n至 100 由本函数 clamp 到界；`before` 是 keyset 游标（#53），取该 id 之前\n更早的一页，非法值服务端回 400）。返回 `count` 是本次返回条数非在册\n总数；返回条数恰打满 limit 时 stderr 出一行截断提示（#52）。\n\n# Errors\n\n网络或超时；服务端非 200（含 before 非法 400）。"
    },
    {
      "path": "browse_cli::issue::new",
      "kind": "function",
      "docs": "提交一条 issue：`POST /api/issues`，回执 `{ok, id, url}`（url 即详情页）。\n\n# Errors\n\n客户端校验不过（title 空或超 200、body 超 20000）；429 限速（每 IP\n每时 10 条）；400 服务端校验；网络或超时。"
    },
    {
      "path": "browse_cli::issue::show",
      "kind": "function",
      "docs": "看 issue 详情：`GET /api/issues/<id>`。\n\n# Errors\n\nid 不存在（404）；网络或超时。"
    },
    {
      "path": "browse_cli::render",
      "kind": "module",
      "docs": "求值结果的打印面：大值自动落盘（artifact/checkpoint 的降级形态，\n用户裁定）。daemon 与 vars 表不动，只保护 agent 上下文不被 10MB JSON 淹没。"
    },
    {
      "path": "browse_cli::render::DROP_THRESHOLD",
      "kind": "constant",
      "docs": "序列化后的打印串超过本阈值（字节）即落盘，保护 agent 上下文。"
    },
    {
      "path": "browse_cli::render::render_or_drop_sync",
      "kind": "function",
      "docs": "渲染求值结果；超阈值时写文件并返回提示行（含路径与预览），\n未超阈值返回正常渲染。落盘文件写原文（字符串不带展示引号），stdout\n展示面才带类型区分（#21）；回执 `bytes` 是工件字节（非展示形态长度）。\n\n# Errors\n\n落盘失败（目录不可写）。\n\n# Examples\n\n```\nuse serde_json::json;\n// 小值正常渲染（字符串带引号，与对象输出可区分）\nassert_eq!(\n    browse_cli::render::render_or_drop_sync(&json!(\"hi\")).unwrap(),\n    \"\\\"hi\\\"\"\n);\n// 大值落盘（同步测试路径）；回执 bytes 是工件字节，不含展示引号\nlet big = json!(\"x\".repeat(browse_cli::render::DROP_THRESHOLD + 1));\nlet line = browse_cli::render::render_or_drop_sync(&big).unwrap();\nassert!(line.contains(\"__dropped\"), \"{line}\");\nlet receipt: serde_json::Value = serde_json::from_str(&line).unwrap();\nassert_eq!(\n    receipt[\"bytes\"].as_u64(),\n    Some(browse_cli::render::DROP_THRESHOLD as u64 + 1)\n);\n```"
    }
  ]
}

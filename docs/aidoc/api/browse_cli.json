{
  "crate": "browse-cli",
  "version": "0.19.1",
  "items": [
    {
      "path": "browse_cli::client",
      "kind": "module",
      "docs": "daemon 客户端：本地 HTTP 调用 + 首次使用自动拉起 detached daemon。"
    },
    {
      "path": "browse_cli::client::DEFAULT_PORT",
      "kind": "constant",
      "docs": "daemon 的缺省端口，`BROWSE_PORT` 与 `BROWSE_NAME` 派生端口可覆盖。\n\n取 9880 是为避开 bh 的 9876。"
    },
    {
      "path": "browse_cli::client::UpParams",
      "kind": "struct",
      "docs": "`engine_up` 的参数面：CLI up 旗标直通（字段名即 /engine/up 请求键）。"
    },
    {
      "path": "browse_cli::client::daemon_alive",
      "kind": "function",
      "docs": "探测 daemon 是否在跑：GET /health 通即为在。"
    },
    {
      "path": "browse_cli::client::daemon_bind",
      "kind": "function",
      "docs": "返回 daemon 监听的 `host:port` 串；多实例由 `BROWSE_NAME` 派生端口（ADR-0006）。\n\n# Examples\n\n```\n# // BROWSE_PORT/BROWSE_NAME 均未设时才是默认口（CI 干净环境成立）\nif std::env::var_os(\"BROWSE_PORT\").is_none() && std::env::var_os(\"BROWSE_NAME\").is_none() {\n    assert_eq!(browse_cli::client::daemon_bind(), \"127.0.0.1:9880\");\n}\n```"
    },
    {
      "path": "browse_cli::client::decode_arg_b64",
      "kind": "function",
      "docs": "解码 `-b/--b64` 通道的片段实参（#22）：标准 base64 解码为 UTF-8\n字符串。PowerShell 与复杂 shell 的引号与编码面一并绕开（方言与\n`--js` 两形态通用）。\n\n# Errors\n\n实参不是合法 base64，或解码后不是 UTF-8 文本（错误带生成 base64 的\n可照抄命令 CTA）。\n\n# Examples\n\n```\nassert_eq!(browse_cli::client::decode_arg_b64(\"cmV0dXJuIDE=\").unwrap(), \"return 1\");\n// 空白容忍（PowerShell 折行形）\nassert_eq!(\n    browse_cli::client::decode_arg_b64(\"cmV0\\ndXJuIDE=\").unwrap(),\n    \"return 1\"\n);\nassert!(browse_cli::client::decode_arg_b64(\"!!!\").is_err());\n```"
    },
    {
      "path": "browse_cli::client::engine_up",
      "kind": "function",
      "docs": "POST /engine/up 显式起引擎，走 ensure 全链（附着优先缺则 spawn）。\n\n# Errors\n\nHTTP 失败或引擎 ensure 失败（连不上、chrome 找不到、spawn 超时）。"
    },
    {
      "path": "browse_cli::client::ensure_daemon",
      "kind": "function",
      "docs": "daemon 不在跑时 detached 拉起 `browse --serve` 并探活到通；已在跑则直接返回。\n\n# Errors\n\n拉起失败（exe 找不到）或 10 秒内 /health 不通。"
    },
    {
      "path": "browse_cli::client::ensure_daemon_with_env",
      "kind": "function",
      "docs": "同 [`ensure_daemon`]，但给新拉起的 daemon 进程注入环境变量\n（#25.1/#25.5：idle-timeout 与代理旗标只影响新 daemon；已在跑的\ndaemon 不动，其口径以启动时为准）。\n\n# Errors\n\ndaemon 起不来或就绪探测超时。"
    },
    {
      "path": "browse_cli::client::eval",
      "kind": "function",
      "docs": "把方言片段 POST 到 daemon 的 /eval 求值。\n\n返回最后一条语句的值；daemon 侧错误进 `Err`（错误串已是给人/agent 的下一步指令形态）。\n\n# Errors\n\nHTTP 失败或 daemon 求值失败（语法错、CDP 错、守卫拦截、超时）。"
    },
    {
      "path": "browse_cli::client::health",
      "kind": "function",
      "docs": "GET /health 取 daemon 状态面；daemon 不在时报可照抄的拉起提示。\n\n# Errors\n\ndaemon 不在或响应坏。"
    },
    {
      "path": "browse_cli::client::quit",
      "kind": "function",
      "docs": "POST /quit 退 daemon，daemon 侧顺带只终结自起引擎（附着来源不动）。\n\n# Errors\n\ndaemon 不在（幂等场景调用方先判活）或 HTTP 失败。"
    },
    {
      "path": "browse_cli::client::skills_passthrough_env",
      "kind": "function",
      "docs": "技能层透传环境（#50/#51）：拉起新 daemon 时把 CLI 进程里的 workspace\n根与两个触发开关显式带给子进程。评审 G4 注记：daemon 由本 crate 的\n`Command` spawn、环境默认整体继承（全仓无 env_clear），三变量只走 env\n不像 `BROWSE_SECRETS` 有 argv 来源，故本透传在现状下与继承等价——保留\n是防御（未来 env_clear 或非 CLI 拉起路径）。「只影响新拉起的 daemon，\n已在跑的以启动时口径为准，改这些配置要重启 daemon」语义不变。\n\n# Examples\n\n```\n// 未设三变量时透传清单为空（不注无谓 env）\nif std::env::var_os(\"BROWSE_WORKSPACE\").is_none() {\n    assert!(browse_cli::client::skills_passthrough_env().is_empty());\n}\n```"
    },
    {
      "path": "browse_cli::client::state_dir",
      "kind": "function",
      "docs": "返回 daemon 日志与运行面目录（`%USERPROFILE%\\.browse-rs[\\<name>]`，多实例各一份）。"
    },
    {
      "path": "browse_cli::fetch",
      "kind": "module",
      "docs": "一次性只读抓取（#50/#56）：HTTP 优先，三条件升级引擎，markdown 直出。\n\n- HTTP 腿：reqwest 直取（零浏览器成本），判升级三条件：正文空、命中\n  墙词、正文少于 20 词。\n- 引擎腿：经 daemon POST /eval 走 goto 加页内抽取（启发式标题加段落，\n  非 Readability；v1 口径已在 surface 披露）。\n- 域名层点名（#56）：两腿回执都按 URL 做 goto 同口径匹配，命中\n  workspace 站点知识附 domain_skills 与 hint；不依赖引擎（引擎腿\n  daemon 侧 goto 的点名回执被抽取串替换，CLI 侧补点）。"
    },
    {
      "path": "browse_cli::fetch::fetch",
      "kind": "function",
      "docs": "抓取入口：HTTP 优先，命中升级条件转引擎腿。\n\n# Errors\n\n两腿全失败（HTTP 网络错且引擎腿也失败）；引擎腿的 goto/抽取错误原样\n上抛。"
    },
    {
      "path": "browse_cli::fetch::needs_upgrade",
      "kind": "function",
      "docs": "升级判定的三条件（#50）：正文空、墙词、少于 20 词。\n纯函数供单测锁。"
    },
    {
      "path": "browse_cli::ledger",
      "kind": "module",
      "docs": "账本薄适配层（REQ-063；总台修正令 2026-09-20 收口）：签名道与只增面\n全在 ledger-client crate（github.com/raystyle/ledger-rs v0.1.1，全舰队\n唯一实现；v0.1.0 有 URL 拼接舰队级缺陷已避），本层只留本仓身份面\n（公钥 JWK 常量与 kid 派生）、密档管理\n（base64url seed，env `BROWSE_LEDGER_PRIVATE_KEY` 或本地密档双通道）、\n命令面本地校验、`--dry-run` 载荷预览与 #52 家族截断提示。CLI 只增不关\n不删：issue close 与 artifact promote/demote/supersede 面已移除，关闭\n与删除唯一道 = 开发工作台经 herdr 委托 omc 工位执行（`omc ledger issue\nstatus <repo> <n> <to>` 与 `omc ledger issue delete`）。真源 =\nledger.ohmygh.com（替代 issues.ohmygh.com 客户端面；旧服务只读保役）。"
    },
    {
      "path": "browse_cli::ledger::ISSUE_KINDS",
      "kind": "constant",
      "docs": "issue kind 集（服务端 ISSUE_KINDS 同源，本地预检省一次网络往返）：bug\n（BUG 错误任务）与 improvement（改进优化任务）。"
    },
    {
      "path": "browse_cli::ledger::PUBKEY_JWK",
      "kind": "constant",
      "docs": "本仓公钥 JWK（REQ-063 裁 2：常量集成进 CLI；字母键序紧凑形，kid 即\n其 sha256hex，与舰队派生约定一致）。私钥恒在提交侧（env 或本地密档），\n不进仓。"
    },
    {
      "path": "browse_cli::ledger::REPO_ID",
      "kind": "constant",
      "docs": "本仓账本身份（REQ-063：repo_id = 规范化 remote）。"
    },
    {
      "path": "browse_cli::ledger::clamp_issue_limit",
      "kind": "function",
      "docs": "issue list 的 limit 钳制（1 至 100，服务端上限；#52 家族标准）：钳制\n单源，命令面默认值与饱和提示判定共用。"
    },
    {
      "path": "browse_cli::ledger::client",
      "kind": "function",
      "docs": "组装标准客户端（签名道与只增面在 ledger-client；本仓身份面注入）。\n\n# Errors\n\n私钥载入失败（见 [`load_keypair`]）。"
    },
    {
      "path": "browse_cli::ledger::issue_list_saturated",
      "kind": "function",
      "docs": "issue list 饱和判定（#52 家族标准）：返回条数不少于钳制后 limit 即示\n警；用 `>=` 不用 `==`，服务端若返回多于请求值，`==` 会静默漏报。"
    },
    {
      "path": "browse_cli::ledger::issue_list_truncation_hint",
      "kind": "function",
      "docs": " issue list 饱和提示行（#52/#53 家族标准）：返回条数打满钳制后 limit\n（或 before 翻页面 has_more 为真）时出此行到 stderr，指向 `--status`\n 不适用（账本列表无状态过滤）故给 `--limit` 提高、`--before` 翻更旧\n 一页与账本网页面；不饱和不出。"
    },
    {
      "path": "browse_cli::ledger::issue_open_dry_run",
      "kind": "function",
      "docs": "issue 开单的 dry-run（#57 G6 评审强制项，账本面保留）：本地同规校验加\n将发送 body 与签名基形预览（ts/nonce/idem 占位），零网络零签名——账本\n只增不可撤且 per-key 日配额，误发测试单不可回收，契约实弹先走这里。\n\n# Errors\n\n本地校验失败（title 空或超 200、kind 出集）；不含任何网络类错误。"
    },
    {
      "path": "browse_cli::ledger::key_id",
      "kind": "function",
      "docs": "kid = sha256hex(规范化 JWK {crv,kty,x}，键序字母、紧凑无空白)；常量\n本身已是该形，直接哈希（单一真相，不在代码里另存 kid 字面量）。"
    },
    {
      "path": "browse_cli::ledger::keygen_write",
      "kind": "function",
      "docs": "密钥对生成（一次性或轮换）：写私钥密档（0600）并返回 (kid, JWK, 旧\nkid)；私钥不打印不进 argv，公钥 JWK 供总台在册（在册后方可写入）。\n在位密档非 `--force` 即拒（防静默销毁）。\n\n# Errors\n\n失败返回 `String` 错误（路径与写入类）。"
    },
    {
      "path": "browse_cli::ledger::load_keypair",
      "kind": "function",
      "docs": "载入账本身份：env `BROWSE_LEDGER_PRIVATE_KEY`（base64url seed）优先，\n次本地密档；构造标准 crate 的 [`ledger_client::KeyPair`]（kid 与 JWK 由\n其按舰队约定派生）。不进 argv 不进仓。\n\n# Errors\n\nenv 值或密档内容非 base64url 32 字节 seed，或密档不可读。"
    },
    {
      "path": "browse_cli::ledger::pairing_ok",
      "kind": "function",
      "docs": "本地私钥与内置公钥 JWK 的配对自检（评审 G3）：密档/env 缺位回 None\n（无法判），在位回配对与否——不配对则写入会全体 401 且难归因。\n\n# Errors\n\n内置 JWK 常量解析失败（形变即测试红，运行时不可达）。"
    },
    {
      "path": "browse_cli::ledger::private_key_path",
      "kind": "function",
      "docs": "私钥密档路径（`~/.browse-rs/ledger/ed25519.key`，内容 = base64url 32 字节\nseed；0600 由 keygen 落）。\n\n# Errors\n\n密档根解析失败。"
    },
    {
      "path": "browse_cli::ledger::validate_artifact_id",
      "kind": "function",
      "docs": "artifact_id 形校验（总台建议 2026-09-20）：36 字 UUID 形\n`8-4-4-4-12` 小写 hex 连四杠；截断或转写错本地即报，免上游 404 往返。\n\n# Errors\n\n非 36 字 UUID 形（段长、非法字符、分隔位）。"
    },
    {
      "path": "browse_cli::ledger::validate_artifact_publish",
      "kind": "function",
      "docs": "artifact 发布入参本地预检（收口批评审 F1）：name trim 后 1 至 200，\n服务端同规；坏入参本地拦免配额损耗。\n\n# Errors\n\nname trim 后空或超 200 字符。"
    },
    {
      "path": "browse_cli::ledger::validate_digest",
      "kind": "function",
      "docs": "digest 校验（服务端 DIGEST_RE 同源）：`sha256:<64hex 小写>` 形合规即 Ok。\n\n# Errors\n\n缺前缀、段长错或大写。"
    },
    {
      "path": "browse_cli::ledger::validate_issue_open",
      "kind": "function",
      "docs": "issue 开单入参本地预检（实发腿与 dry-run 同规，收口批评审 F1）：坏\n入参本地拦（exit 2 语义），免打到服务端吃 400 且损耗 per-key 日配额。\n\n# Errors\n\ntitle trim 后空或超 200 字符；kind 出 [`ISSUE_KINDS`] 集。"
    },
    {
      "path": "browse_cli::render",
      "kind": "module",
      "docs": "求值结果的打印面：大值自动落盘（artifact/checkpoint 的降级形态，\n用户裁定）。daemon 与 vars 表不动，只保护 agent 上下文不被 10MB JSON 淹没。"
    },
    {
      "path": "browse_cli::render::DROP_THRESHOLD",
      "kind": "constant",
      "docs": "序列化后的打印串超过本阈值（字节）即落盘，保护 agent 上下文。"
    },
    {
      "path": "browse_cli::render::render_or_drop_sync",
      "kind": "function",
      "docs": "渲染求值结果；超阈值时写文件并返回提示行（含路径与预览），\n未超阈值返回正常渲染。落盘文件写原文（字符串不带展示引号），stdout\n展示面才带类型区分（#21）；回执 `bytes` 是工件字节（非展示形态长度）。\n\n# Errors\n\n落盘失败（目录不可写）。\n\n# Examples\n\n```\nuse serde_json::json;\n// 小值正常渲染（字符串带引号，与对象输出可区分）\nassert_eq!(\n    browse_cli::render::render_or_drop_sync(&json!(\"hi\")).unwrap(),\n    \"\\\"hi\\\"\"\n);\n// 大值落盘（同步测试路径）；回执 bytes 是工件字节，不含展示引号\nlet big = json!(\"x\".repeat(browse_cli::render::DROP_THRESHOLD + 1));\nlet line = browse_cli::render::render_or_drop_sync(&big).unwrap();\nassert!(line.contains(\"__dropped\"), \"{line}\");\nlet receipt: serde_json::Value = serde_json::from_str(&line).unwrap();\nassert_eq!(\n    receipt[\"bytes\"].as_u64(),\n    Some(browse_cli::render::DROP_THRESHOLD as u64 + 1)\n);\n```"
    }
  ]
}
